spring:
  application:
    name: security-service
  data:
    mongodb:
      uri: mongodb://santosh:passw0rd@localhost:27017/flower
      connection-pool-size: 10
      driver-class-name: org.mongodb.driver.MongoDriver
  security:
    user:
      name: admin
      password: adminpassword
      roles: USER